# Mark `pages` as Python package — Streamlit MPA system scan này, nhưng để
# dynamic imports (vd. `from pages.tools_page_C.history_score_regime import ...`
# trong C_Behavioral_Finance.py) hoạt động, package phải có __init__.
